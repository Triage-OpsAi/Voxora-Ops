"use client";
import {useQuery} from "@tanstack/react-query";import {Bot,BrainCircuit,PhoneCall,Timer} from "lucide-react";import {api} from "@/lib/api";import {duration} from "@/lib/utils";import {PageHeader} from "@/components/page-header";import {Card} from "@/components/ui/card";import {Badge} from "@/components/ui/badge";import {Skeleton} from "@/components/ui/skeleton";
export function AgentsView(){const{data,isLoading}=useQuery({queryKey:["agents"],queryFn:api.agents});return <><PageHeader title="AI agents" description="Inspect the models, prompts, voices, health, and operating performance of your deployed workforce." actions={<button className="rounded-xl bg-white px-3 py-2 text-xs font-semibold text-black">+ Create agent</button>}/>{isLoading?<Skeleton className="h-80"/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data?.map(a=><Card key={a.id} className="p-5 transition hover:-translate-y-1 hover:border-violet-400/20"><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-2xl bg-gradient-to-br from-violet-500/20 to-cyan-400/5 text-violet-300"><Bot size={20}/></span><Badge tone="success"><span className="mr-1.5 size-1.5 rounded-full bg-emerald-400"/>{a.health}</Badge></div><h2 className="mt-5 font-medium">{a.name}</h2><p className="mt-1 text-xs capitalize text-zinc-600">{a.niche.replace("_"," ")} workflow</p><div className="mt-5 grid grid-cols-3 gap-2"><Metric icon={<PhoneCall/>} value={String(a.call_count)} label="Calls"/><Metric icon={<Timer/>} value={duration(a.average_duration)} label="Average"/><Metric icon={<BrainCircuit/>} value={`${a.success_rate}%`} label="Success"/></div><div className="mt-5 space-y-3 border-t border-white/[.06] pt-4 text-xs"><Row label="Model" value={a.model}/><Row label="Voice" value={a.voice}/><Row label="Prompt" value={a.prompt_version}/></div></Card>)}</div>}</>}
function Metric({icon,value,label}:{icon:React.ReactNode;value:string;label:string}){return <div className="rounded-xl bg-white/[.025] p-3 [&_svg]:mb-2 [&_svg]:size-13 [&_svg]:text-zinc-600"><>{icon}</><div className="text-sm font-medium tabular">{value}</div><div className="mt-1 text-[9px] text-zinc-600">{label}</div></div>};function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-3"><span className="text-zinc-600">{label}</span><span className="truncate text-zinc-300">{value}</span></div>}
